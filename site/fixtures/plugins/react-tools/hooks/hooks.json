{
  "hooks": {
    "PostToolUse": [
      {
        "name": "on-save-format",
        "description": "Hook, що запускає форматер після кожного збереження файлу.",
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/scripts/format.sh" }
        ]
      }
    ]
  }
}

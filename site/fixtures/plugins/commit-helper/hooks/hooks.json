{
  "hooks": {
    "PreToolUse": [
      {
        "name": "pre-commit-lint",
        "description": "Hook that validates the commit message before writing.",
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/scripts/lint-commit-msg.sh" }
        ]
      }
    ]
  }
}

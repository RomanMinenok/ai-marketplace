{
  "hooks": {
    "PreToolUse": [
      {
        "name": "pre-push-scan",
        "description": "Hook that runs a quick scan before push.",
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/scripts/pre-push-scan.sh" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "name": "secret-guard",
        "description": "Hook that prevents committing keys and tokens.",
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/scripts/secret-guard.sh" }
        ]
      }
    ]
  }
}
